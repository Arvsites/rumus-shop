92-51-22-168.sslip.io {
    encode gzip

    @api path /api/* /health
    handle @api {
        reverse_proxy backend:8000
    }

    handle {
        root * /srv/static
        try_files {path} /index.html
        file_server
    }

    log {
        output stdout
        format console
    }
}
